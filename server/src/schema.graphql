scalar DateTime

enum LinkOrderByInput {
  createdAt_ASC
  createdAt_DESC
}

type Query {
  info: String!
  todoes(orderBy: LinkOrderByInput): [Todo!]!
}

type Mutation {
  createTodo(task: String!): Todo!
}

type Todo {
  id: ID!
  createdAt: DateTime!
  done: Boolean!
  task: String!
}